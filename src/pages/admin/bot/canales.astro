---
import BotAdminLayout from '@/features/bot-admin/components/BotAdminLayout.astro';
import ChannelsManager from '@/features/bot-admin/components/ChannelsManager.tsx';
import { convex } from '@/features/bot-admin/lib/convexClient';
import { isAdminPage } from '@/features/bot-admin/lib/pageAuth';

if (!await isAdminPage(Astro)) return Astro.redirect('/');

let initialChannels: any[] = [];
try {
  initialChannels = await convex.query('channelConfig:listAll' as any, {});
} catch (_) {}
---

<BotAdminLayout title="Configuración de Canales" active="canales">
  <ChannelsManager client:load initialChannels={initialChannels} />
</BotAdminLayout>
